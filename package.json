{
  "name": "comments-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "heroku local -f Procfile.dev -e .env.development",
    "all:dev": "npm run server:dev & npm run client:dev",
    "all:prod": "npm run client:build && npm run server:build && npm run server:prod",
    "server:dev": "cd server && npm run dev",
    "server:prod": "cd server && npm run start",
    "server:build": "cd server && npm run build",
    "client:dev": "cd client && npm run start",
    "client:build": "cd client && npm run build",
    "heroku-prebuild": "cd server &&  npm install && cd ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "cacheDirectories": [
    "client/node_modules",
    "server/node_modules"
  ]
}
